# encoding: utf-8
# from map.views import test
from smartpipe import views

__author__ = '韦总、杨中威'
__date__ = '2019/12/20'

from django.urls import path, re_path
from django.conf.urls import url

app_name = "installinfo"

urlpatterns = [

    # 百度地图
    # path('smartpipe/', index, name="smartpipe"),
    # url(r'index', views.index),
    # re_path('pipe/(?P<meter_id>\d+)/', views.pipe.as_view(), name="pipe"),
    # url(r'$', views.test),
]